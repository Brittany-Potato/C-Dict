//* Contains the 'Main' method, which is the entry point in most C# applications.

// 'using' directives bring in namespaces, which are collections of related code.
// 'system' namespace contains fundamental classes and base types.
using System;

// A namespace is used to organize your code and prevent naming conflicts.
namespace MyFirstCSharpApp
{
    // A class is a blueprint for creating objects.
    class Program
    {
        // The Main method is the starting point of your application.
        static void Main(string[] args)
        {
            // Console.WriteLine() is a Method that prints text to console.
            Console.WriteLine("Hello, C# World");
        }
    }
}